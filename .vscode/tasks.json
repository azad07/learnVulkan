{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Engine",
            "osx": {
                "command": "${workspaceFolder}/build-macos.sh"
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}